name: flutter_state_management
description: A collection of helper classes to make the use of ChangeNotifier easier for state management
version: 0.7.2
homepage: https://github.com/MohiuddinM/flutter_state_management

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mockito: ^5.0.0